// Page-specific configuration for Georgia-Gardner
// This file defines which questions to use and page-specific settings

const PAGE_CONFIG = {
  pageName: "Georgia-Gardner",
  questionSet: "Georgia-Gardner",
  pageTitle: "Georgia Gardner - Voice Recording Interface",

  // Styling overrides (optional)
  themeColor: "#000000",

  // Metadata
  description:
    "Voice recording interface for Georgia Gardner's exhibition feedback",
};

// Export for use in main script
window.PAGE_CONFIG = PAGE_CONFIG;
