// Page-specific configuration for dmstfctn
// This file defines which questions to use and page-specific settings

const PAGE_CONFIG = {
  pageName: "dmstfctn",
  questionSet: "dmstfctn",
  pageTitle: "dmstfctn - Voice Recording Interface",

  // Styling overrides (optional)
  themeColor: "#000000",

  // Metadata
  description: "Voice recording interface for dmstfctn's exhibition feedback",
};

// Export for use in main script
window.PAGE_CONFIG = PAGE_CONFIG;
